services:
  front:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://204.168.212.239}
    container_name: virtual-store-front
    restart: unless-stopped
    ports:
      - "${FRONT_PORT:-5173}:80"
